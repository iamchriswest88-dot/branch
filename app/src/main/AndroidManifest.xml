<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Glyph Matrix SDK permission -->
    <uses-permission android:name="com.nothing.ketchum.permission.ENABLE"/>
    <!-- Calendar mirror -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
    <!-- Network access for Supabase Sync -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".BranchApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Branch">

        <meta-data android:name="NothingKey" android:value="test"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Glyph Toy Service - drives Glyph Button and AOD carousel -->
        <service
            android:name=".glyph.BranchGlyphService"
            android:exported="true"
            android:permission="com.nothing.ketchum.permission.ENABLE">
            <intent-filter>
                <action android:name="com.nothing.ketchum.action.GLYPH_TOY"/>
            </intent-filter>
            <meta-data
                android:name="com.nothing.ketchum.metadata.GLYPH_TOY"
                android:value="true"/>
        </service>

        <receiver
            android:name=".widget.BranchWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/branch_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.GymWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gym_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.FlowWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/flow_widget_info" />
        </receiver>

    </application>

</manifest>
